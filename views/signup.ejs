<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="" />
    <meta charset="ISO-8859-1" />
    <meta
      name="author"
      content="Mark Otto, Jacob Thornton, and Bootstrap contributors"
    />
    <meta name="generator" content="Hugo 0.88.1" />
    <title>Findit-logiin</title>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    <link
      rel="canonical"
      href="https://getbootstrap.com/docs/5.1/examples/sign-in/"
    />

    <!-- Bootstrap core CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />

    <style>
      :root {
        --red: #ff5a3c;
        --light-red: #f9edeb;
      }

      .form-div {
        border: 0.1rem solid rgba(0, 0, 0, 0.2);
        background-color: #fff;
        padding: 10%;
        border-radius: 2%;
        overflow: hidden;
        flex: 1 1 30rem;
        border: 0.1rem solid rgba(0, 0, 0, 0.2);
        box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.1);
      }

      .show {
        margin: 20px 0;
        width: 1.2rem;
        height: 1.2rem;
      }

      html,
      body {
        height: 100%;
      }

      body {
        position: relative;
        display: flex;
        align-items: center;
        padding-top: 40px;
        padding-bottom: 40px;
        background-color: #f5f5f5;
        font-family: "Nunito", sans-serif;
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        outline: none;
        border: none;
        text-decoration: none;
      }

      .top-body {
        background: linear-gradient(
          to bottom right,
          white 30%,
          var(--light-red) 70%
        );
      }

      .form-signin {
        width: 100%;
        max-width: 430px;
        padding: 15px;
        margin: auto;
      }

      .form-signin .checkbox {
        font-weight: 400;
      }

      .form-signin .form-floating:focus-within {
        z-index: 2;
      }

      .head-text {
        color: black;
        font-style: bold;
        font-size: 2rem;
      }

      .top {
        margin-top: 30px;
        margin-bottom: -1px;
        border-bottom-right-radius: 0;
        border-bottom-left-radius: 0;
      }

      .middle {
        margin-top: 0;
        margin-bottom: 30px;
        border-radius: 0;
      }

      .bottom {
        border-top-left-radius: 0;
        border-top-right-radius: 0;
      }

      .bd-placeholder-img {
        font-size: 1.125rem;
        text-anchor: middle;
        -webkit-user-select: none;
        -moz-user-select: none;
        user-select: none;
      }

      .btn {
        display: inline-block;
        border-radius: 0.5rem;
        font-size: 1.2rem;
        color: var(--red);
        margin-top: 20px;
        background: #fff;
        cursor: pointer;
        text-align: center;
        border: 1px solid black;
      }

      .btn:hover {
        background: var(--red);
        color: #fff;
      }

      .heading span {
        color: #ff5a3c;
        border-radius: 0.5rem;
        padding: 1.5rem 0.5rem;
      }

      .fail {
        position: absolue;
        bottom: 20px;
        left: 20px;
        padding: 1%;
        background-color: crimson;
        color: #f5f5f5;
        align-items: center;
        text-align: center;
        display: block;
      }

      .show {
        margin: 10px;
        width: 20px;
        height: 20px;
      }

      .error {
        background-color: #ff4444;
        padding: 10px 20px;
        border-radius: 20px;
        font-size: 1.5rem;
        margin-bottom: 20px;
        color: white;
      }

      .success {
        visibility: hidden;
        display: block;
      }

      .captchaText {
        height: 100%;
        border-radius: 5px 0 0 5px;
        width: 40%;
        text-align: center;
        text-decoration: line-through;
      }

      .captchaInput {
        width: 60%;
        border-radius: 0 5px 5px 0;
        height: 100%;
        border: 2px solid var(--red);
      }
      .captchaInput:focus {
        outline: none;
        border: 1px solid var(--red);
      }

      .captcha {
        height: 30px;
        margin: 10px 0;
        display: flex;
        width: 100%;
        justify-content: space-around;
      }

      @media (max-width: 991px) {
        .form-signin {
          width: 90%;
        }
      }
    </style>
  </head>

  <body class="text-center top-body">
    <main class="form-signin">
      <form action="/login" method="post">
        <div class="form-div">
          <% if(p!='') { %>
          <button class="error" disabled><%= p[p.length-1] %></button>
          <% } %>
          <h1 class="heading"><span>Welcome</span>Back</h1>

          <div class="form-floating">
            <input
              type="email"
              name="mail"
              class="form-control top"
              id="floatingInput"
              placeholder="name@example.com"
              required
            />
            <label for="floatingInput">Email address</label>
          </div>
          <div class="form-floating">
            <input
              minlength="8"
              type="Password"
              name="pass"
              class="form-control middle"
              id="floatingPassword"
              placeholder="Password"
              required
            />
            <label for="floatingPassword">Password</label>
            <input class="show" type="checkbox" onclick="myFunction()" /> Show
            Password
          </div>
          <div class="captcha">
            <input class="captchaText" value="<%= captcha %>" disabled />
            <input class="captchaInput" type="text" id="captchaText" required />
          </div>

          <button
            class="w-100 btn"
            ID="signUpButton"
            name="signup-button"
            type="submit"
          >
            Sign In
          </button>
          <div>
            <a
              style="color: black; font-weight: 700"
              class="nav-link"
              href="/signup"
              >Sign Up Instead</a
            >
          </div>
          <h5 style="color: black" class="mt-5 mb-3">&copy; 2020–2022</h5>
        </div>
      </form>
    </main>
  </body>
  <script>
    function myFunction() {
      var x = document.getElementById("floatingPassword");
      if (x.type === "password") {
        x.type = "text";
      } else {
        x.type = "password";
      }
    }
    function handleValidation(evt) {
      return captcha;
    }
    document.addEventListener("submit", (evt) => {
      evt.preventDefault();
      if ("<%=captcha%>" === document.getElementById("captchaText").value) {
        $("form").submit();
      } else {
        alert("Incorrect captcha");
      }
    });
  </script>
</html>
